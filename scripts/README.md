Project is under construction ...

Useful maintenance command:

```bash
julia scripts/00-extract_chapter_code.jl
```

To clean old rendered output manually before rebuilding, remove `docs/`, `.quarto/`, and `_freeze/` from the repository root and then rerun `quarto render`.